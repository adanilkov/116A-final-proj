'use client';

import React, { useState } from 'react';
import SegmentedBarChart from './barchart.jsx'; // 
import MapVis from './map.jsx'; // 

const LinkedVisualization = () => {
  const [selectedRegions, setSelectedRegions] = useState([]); // Shared state for brushing
  const [selectedCategory, setSelectedCategory] = useState(null); // Shared state for bar chart selection

  return (
    
  );
};

export default LinkedVisualization;
